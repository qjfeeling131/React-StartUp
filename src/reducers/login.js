import { Map } from "immutable";

const initState=Map({
    authUser:{

    },
    user:{

    }
})

const loginReducer=(state=initState,action)=>{
    switch(action.type)
    {
        
    }
}

export default loginReducer